# 🚀 Быстрый старт Expense Tracker

## ⚡ Запуск за 5 минут

### 1️⃣ Создайте Telegram бота

1. Откройте Telegram → Найдите `@BotFather`
2. Отправьте `/newbot`
3. Введите имя: `Expense Tracker`
4. Введите username: `your_expense_tracker_bot`
5. **Скопируйте токен:** `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2️⃣ Получите ваш Telegram ID

1. Найдите бота `@userinfobot`
2. Отправьте `/start`
3. **Скопируйте ID:** `123456789`

### 3️⃣ Настройте .env файл

```bash
# Скопируйте шаблон
cp env.example .env
```

Откройте `.env` и заполните:

```env
# Database
POSTGRES_PASSWORD=ваш_пароль_123

# Telegram (ВАЖНО!)
TELEGRAM_BOT_TOKEN=вставьте_токен_из_шага_1
TELEGRAM_WHITELIST=вставьте_id_из_шага_2

# Генерируйте случайные ключи (32 символа)
BOT_API_KEY=случайная_строка_32_символа
JWT_SECRET=другая_случайная_строка_32
```

**Генерация случайных ключей (PowerShell):**
```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
```

### 4️⃣ Запустите проект

```bash
# Запуск всех сервисов
docker-compose up --build -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

### 5️⃣ Проверьте работу

**Telegram бот:**
1. Найдите бота: `t.me/your_expense_tracker_bot`
2. Отправьте `/help`
3. Попробуйте: `100` или `100 продукты`

**Веб-интерфейс:**
1. Откройте: http://localhost
2. Войдите через Telegram (или "Simulate Login" для теста)
3. Посмотрите графики и статистику

---

## 📊 Что уже работает

✅ **База данных:**
- PostgreSQL с индексами для быстрых запросов
- Автоматическая инициализация схемы
- Health checks

✅ **API Service:**
- REST API на Go
- JWT аутентификация
- Telegram Login Widget
- Автоопределение категорий
- Health check endpoint

✅ **Bot Service:**
- Telegram бот с улучшенным логированием
- Поддержка команд: /help, /total, /debts
- Автоматическое определение категорий
- Обработка сумм с копейками (100.50 или 100,50)

✅ **Frontend:**
- React + TypeScript + Vite
- **Графики Chart.js:**
  - График расходов за 7 дней (Line chart)
  - Распределение по категориям (Pie chart)
- **Фильтры:**
  - По категориям
  - По периодам (все/неделя/месяц)
- **Категории в UI:**
  - Выбор при добавлении расхода
  - Отображение в списке расходов
- Responsive дизайн
- Telegram Login Widget

✅ **Инфраструктура:**
- Docker Compose с health checks
- Автоматический перезапуск сервисов
- Правильные зависимости между контейнерами

---

## 🎯 Основные функции

### Telegram бот

```
/help          - Справка
/total         - Общая сумма расходов
/total week    - Расходы за неделю
/total month   - Расходы за месяц
/debts         - Показать долги

100            - Записать расход 100 руб.
100 продукты   - Расход с категорией
50.50 кафе     - Расход с копейками
```

### Веб-интерфейс

- 📊 Графики (7 дней, по категориям)
- 🔍 Фильтры (период, категории)
- ➕ Добавление расходов с категорией
- 📋 Список последних расходов
- 🔐 Вход через Telegram

### Категории (автоопределение)

- 🛒 Продукты (еда, магазин, супермаркет)
- 🚗 Транспорт (бензин, такси, метро)
- ☕ Кафе и рестораны (кафе, обед, кофе)
- 🎭 Развлечения (кино, театр, игры)
- 💊 Здоровье (аптека, врач, лекарства)
- 👔 Одежда (одежда, обувь, шопинг)
- 🏠 Коммунальные услуги (свет, вода, интернет)
- 📦 Прочее

---

## 🔧 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f bot      # Логи бота
docker-compose logs -f api      # Логи API
docker-compose logs -f db       # Логи БД

# Перезапуск сервиса
docker-compose restart bot
docker-compose restart api

# Остановка
docker-compose down

# Полная пересборка
docker-compose down
docker-compose up --build -d

# Подключение к БД
docker-compose exec db psql -U expense_user -d expense_tracker
```

---

## 🐛 Решение проблем

### Бот не отвечает?

```bash
# Проверьте логи
docker-compose logs bot

# Убедитесь что:
# 1. TELEGRAM_BOT_TOKEN правильный (из @BotFather)
# 2. TELEGRAM_WHITELIST содержит ваш ID
# 3. Нет лишних пробелов в .env

# Перезапустите бота
docker-compose restart bot
```

### Веб не загружается?

```bash
# Проверьте статус
docker-compose ps

# Все контейнеры должны быть "Up" и "healthy"
# Если нет - проверьте логи:
docker-compose logs api
docker-compose logs frontend
```

### Ошибка подключения к БД?

```bash
# Проверьте health БД
docker-compose ps db

# Должен быть: Up (healthy)
# Если нет:
docker-compose restart db
docker-compose logs db
```

---

## 📚 Дополнительная документация

- 📘 **TELEGRAM_SETUP_GUIDE.md** - Подробная инструкция по Telegram (для новичков)
- 📗 **QUICKSTART.md** - Быстрый старт проекта
- 📙 **DEPLOY.md** - Развертывание на сервере
- 📕 **SETUP_GITHUB.md** - Развертывание с GitHub

---

## ✨ Что НЕ реализовано (оставлено на потом)

- ❌ OCR обработка чеков (заглушка)
- ❌ Shared expenses через бот (частично)
- ⚠️ Unit тесты (минимальные)
- ⚠️ Redux (не требуется для текущего объема)
- ⚠️ Material-UI (используется custom CSS)

**Приоритет:** Сначала основной функционал работает без OCR!

---

## 🎉 Готово к использованию!

Проект полностью функционален:
- ✅ Запись расходов через Telegram
- ✅ Веб-интерфейс с графиками
- ✅ Автоопределение категорий
- ✅ Фильтры и статистика
- ✅ База данных с индексами
- ✅ Health checks

**Начните использовать прямо сейчас!** 🚀

Запишите первый расход через Telegram:
```
100 продукты
```

Затем откройте http://localhost и посмотрите графики! 📊

